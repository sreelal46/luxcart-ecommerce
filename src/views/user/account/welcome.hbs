<link rel="stylesheet" href="/css/user/account/welcome.css">

<section id="welcome" class="main-card mt-4 hero">
  <div class="welcome-card">
    <div class="welcome-visual">
      <!-- use uploaded image as avatar -->
      <div class="welcome-avatar">
        <img src="{{user.profileImage_url}}" alt="avatar">
      </div>
    </div>

    <div class="welcome-content">
      <h2 class="welcome-title">
        Welcome back, <small id="welcomeName">{{user.name}}</small> <span aria-hidden="true">👋</span>
      </h2>

      <p class="welcome-text">
        This is your account hub — update your profile, track orders, manage your wishlist and addresses,
        and access any account tools from the left menu. Everything is stored in one place for quick access.
      </p>

      <div class="welcome-actions">
        <button class="btn btn-dark" id="goToProfileBtn">Go to Profile</button>
        <button class="btn btn-ghost" id="goToOrdersBtn">View Orders</button>
      </div>
    </div>
  </div>
</section>

<script src="/js/user/account/welcome.js"></script>