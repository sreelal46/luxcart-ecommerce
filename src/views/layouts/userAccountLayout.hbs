<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link
    href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&family=Playfair+Display:wght@400;700&display=swap"
    rel="stylesheet">
  <!-- Cropper.js CSS/JS and Axios -->
  <link href="https://unpkg.com/cropperjs@1.5.13/dist/cropper.min.css" rel="stylesheet" />
  <script src="https://unpkg.com/cropperjs@1.5.13/dist/cropper.min.js"></script>
  <!-- axios -->
  <script src="/common/axios.min.js" defer></script>

  <link rel="stylesheet" href="/common/bootstrap.min.css">
  <!-- Bootstrap 5 CSS -->
  <link rel="stylesheet" href="/bootstrap-icons/bootstrap-icons.css">

  <title>{{title}}</title>

  <style>
    /* Header / page title — replacement styles */
    .account-header {
      display: flex;
      /* same as your d-flex wrapper */
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-bottom: 18px;
      /* header spacing controlled here */
    }

    /* Title */
    .page-title {
      margin-top: 70px;
      /* remove top/bottom margins so it's vertically centered */
      font-weight: 700;
      font-size: 28px;
      line-height: 1.05;
      color: var(--accent);
    }

    /* Mobile back button (hidden on md and up; visible on small screens) */
    #mobileBackBtn {
      display: none;
      /* default hidden (desktop) */
      align-items: center;
      justify-content: center;
      padding: 6px 8px;
    }

    /* Show back button on small screens and reduce title size */
    @media (max-width: 767.98px) {
      #mobileBackBtn {
        display: inline-flex;
        /* visible on small screens */
      }

      .page-title {
        font-size: 20px;
      }
    }
  </style>
</head>

<body>
  {{> navbar}}
  <main class="container-lg mt-4 mb-5">
    <div class="d-flex align-items-center justify-content-between mb-3 account-header">
      <button id="mobileBackBtn" class="btn btn-sm d-md-none">
        <i class="bi bi-arrow-left"></i>
      </button>

      <h2 class="page-title">My Account</h2>
    </div>


    <div class="row g-4">

      <!-- sidebar -->
      {{> userAccountSidebar}}

      <div class="col-lg-9">

        {{{body}}}

      </div>

    </div>

  </main>



  {{> footer}}


  <!-- GSAP & ScrollTrigger -->
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/gsap.min.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/ScrollTrigger.min.js" defer></script>


  <!-- Bootstrap JS -->
  <script src="/common/bootstrap.bundle.min.js" defer></script>


  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</body>

</html>